using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GameSaver : MonoBehaviour
{
    public ShopCategoryManager ThemeManager;

    void Start()
    {
        //ChooseableShopCard.actualChoosen = ThemeManager.cardsParent.GetChild(0).GetComponent<ChooseableShopCard>();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
